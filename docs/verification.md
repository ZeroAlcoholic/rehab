# 驗證紀錄

## 2026-09-22：匯入結果直接可見（v29）

使用者反映選檔後沒有反應。隔離正式站以私人備份匯入兩次均成功且冪等，未重現資料驗證或寫入失敗，不能由此推斷使用者的實際匯入結果。選檔區新增處理中、儲存後筆數與失敗原因，完成後聚焦提示，提供展開紀錄庫的「查看紀錄」按鈕；匯入已有試算表來源的備份後即更新目前設定視窗的綁定狀態。

import_feedback_check 驗證可見且聚焦的筆數、重複匯入、查看紀錄、格式及來源試算表錯誤保留資料；修正對話框按鈕 display 覆蓋 hidden 的樣式。settings_auth、browser 及 public_update 通過。測試使用合成資料；私人備份與診斷輸出保留 private/，不隨發布公開。

## 2026-09-22：既有使用者更新入口（v28）

重現 v26 頁面在新版 worker 等待時，F5 仍受舊 worker 控制，連線後按鈕仍可按。前一輪全新設定檔的正式站驗證未涵蓋此情況。新增不列入離線快取的 update.html，沿用既有更新握手與其他分頁保護，顯示實際生效版本後提供返回入口；不清除 IndexedDB，不匯入或上傳私人資料。

public_update_check 驗證單分頁更新、保護另一分頁未存草稿、下載失敗重試、更新前後完整儲存狀態相同及更新後離線使用；原 shell_upgrade 與 settings_auth 通過。此為更新流程證據，尚不能斷定使用者當下的實際分頁狀態或真實 Google 帳號授權結果。

## 2026-09-22：F5 授權續用與已連線狀態（v27）

短效 Google 授權改存分頁 sessionStorage，啟動時核對 Client ID、scope 與原到期時間，有效才恢復並同步原 Sheet。連線成功按鈕顯示「已連線」並停用；重複呼叫不重開 Google，重新授權不強制選帳號。到期、中斷、更換 Client ID 或 API 401 清除暫存；設定視窗同步反映失效，重新準備後可直接再連線。瀏覽器禁止暫存時明示 F5 限制。

新增測試先重現缺少續用與已連線狀態，再驗證修正。99 項 Node 與 46 個模組邊界通過；settings_auth_check 在真實 Chrome 驗證 F5 不再請求授權、已連線按鈕停用、分頁內到期、中斷、還原後 401 直接重連、Client ID 切換。browser_check 確認重載直接同步原 Sheet、無重複紀錄，匯出備份不含 token；shell_upgrade 與 onboarding 通過。獨立審查找到還原後 401 準備狀態缺陷，已補測並修正。

Google SDK／HTTP 使用受控回應，並非真實帳號或 Android 實機驗證。此修正支援有效期內的 F5，不承諾永久登入、跨分頁授權共用或關閉後背景同步；sessionStorage 的安全取捨見設定文件。

## 2026-09-22：正式發布前驗證（v26）

95 項 Node 測試、46 個模組邊界與 15 支 Chrome 瀏覽器流程全部通過，包含備份匯入、記錄讀回、離線、舊版更新保留資料及授權介面。Google 邊界採受控回應；真實 Google 帳號與 Android 實機仍未驗證。發布使用 Pages workflow 的公開資產白名單，私人備份不隨網站發布，須另在正式站匯入或透過私人 Sheet 同步。下方各節的「尚未部署」描述為當時驗證狀態；實際發布結果以 GitHub Actions 及線上版本為準。

## 2026-09-22：Google 登入狀態與錯誤診斷（v26，本機）

正式 v15 在 Chrome 手機尺寸測試中開啟主頁加一個 Google 登入頁，未重現兩個 Google 視窗。已確認授權期間仍可修改 Client ID／準備／中斷，畫面仍顯示尚未連線；中斷後登入視窗仍存在，取消後錯誤位置超出手機可見範圍。

修正授權期間的控制項與 Escape 保護、等待狀態、鄰近連線區的錯誤與焦點；更換 Client ID 後需重新準備。已知 OAuth 錯誤使用固定分類，不顯示原始錯誤描述或 token。settings_auth_check 先重現缺少等待狀態，再驗證手勢同步、鎖定、取消恢復、錯誤可見、重試成功及 Client ID 切換。

這些證據確認介面與授權流程防護，不能確認使用者手機上的帳號／Cloud 設定錯誤已修復。真實帳號登入、授權後 callback、Android PWA 往返仍待實機錯誤與驗證；這版尚未部署。參考 Google [token model](https://developers.google.com/identity/oauth2/web/guides/use-token-model) 及 [error handling](https://developers.google.com/identity/oauth2/web/guides/error)。

## 2026-09-22：校準待核對提示（v25）

器材入口以短標籤及可及名稱顯示既有待核對欄位，記錄表單提供展開既有核對設定的按鈕。勾選狀態改變時即更新提示，不自行解除資料標記。當日摘要加上「已記錄」，避免把現有組數解讀成全天完整訓練量。

visual_training 先重現卡片缺少姿勢提示，再驗證標籤、查看入口、勾選即時更新、表單正常關閉及手機版面。94 項 Node、46 模組邊界及 visual_training、gym_entry、training_day、training_flow、submit_recovery、forest_ux、browser、shell_upgrade 通過。獨立聚焦 review 無阻斷發現。私人校正資料另於本機核對：不同組數或有待核對條件時不直接判定進退，紀錄與原始版本保留；無私人數據加入公開測試。

## 2026-09-22：完工 review（v24）

獨立程式審查未發現新的阻斷問題。實際表單檢查補上各組負荷單位與掛片語意：重量／輔助／掛片欄位就近標示 kg、lb，快速記錄掛片時明示不含起始阻力。新增瀏覽器斷言先重現缺少標示，再確認修正。

最終重跑 94 項 Node 測試、46 模組邊界檢查，以及 visual_training、gym_entry、training_day、submit_recovery、training_flow、forest_ux、browser、shell_upgrade、body_map、atlas_ux、longitudinal、history_disclosure、rehab、onboarding 共 14 支 Chrome 流程，均通過。另以已匯入的私人資料檢查 320／412／1440px 與 100%／200% 字級，頁面及快速記錄視窗無橫向溢出，沿用前次後的本次次數仍留白。

此為本機完工驗證；Android 實機键盤與操作體感、真實 Google 授權仍未涵蓋。Google 流程使用受控 HTTP 回應。未提交、推送或部署。

## 2026-09-22：器材辨識與首屏整理（v23）

既有 SVG 增加機架、靠墊、配重及人物分層，仍是通用示意而非特定機型照片；徒手深蹲不顯示機台。導覽移至清單前方，搜尋與部位篩選預設收合並保留已選條件。快速記錄顯示動作圖、獨立機台名稱及分行日期，說明收進既有設定。

visual_training 新增首頁第一張器材完整可見、篩選收合、導覽位置、表單身分與切換動作換圖的瀏覽器斷言。visual_training、gym_entry、forest_ux、training_flow、training_day、browser、shell_upgrade 與 46 模組邊界檢查通過。檢視全動作圖及私人紀錄的手機尺寸畫面。Android 實機與真實 Google 帳號仍未驗證；未部署。

## 2026-09-22：健身房記錄與當日部位摘要

- 94 項 Node 測試通過，46 個模組通過邊界檢查。新摘要沿用既有肌群對應，沒有增加訓練效果或醫療判讀規則。
- `visual_training_check.py`：目錄動作示意圖、正背面主要／協同上色、靜態圖不含小字或互動焦點、來源展開、逐組變動負荷、同重合併呈現、輔助／掛片標示、同機台不同單位辨識、備註收合、唯一 SVG ID，以及 320／412／1100px、200% 文字通過。
- v20 更新預快取改為重新取得資源。`shell_upgrade_check.py` 新增仍在 HTTP 有效期限內的舊資產，先重現新殼層混入舊程式，再驗證修正；保留未同步紀錄、其他分頁草稿、下載失敗重試與離線讀回案例。
- `gym_entry_check.py`：器材搜尋／部位入口、逐組前次參考、條件變更、移除組別對應、明確填入次數、儲存讀回、取消／確認捨棄與手機版面通過。
- `training_day_check.py`：今天空狀態不冒用舊紀錄、最近一次查找、主要／協同來源、暖身排除、直接修改、跨概覽日期篩選、儲存後切至紀錄日期、重載保留資料、只有版本衝突的日期與 320／412px 放大 200% 通過。
- `submit_recovery_check.py`：同一對話框重複送出只執行一次、儲存期間鎖定輸入與 Escape、失敗保留值並恢復原停用狀態、再次送出成功通過。這是受控失敗案例；真正 IndexedDB 流程由下列回歸測試覆蓋。
- 已重跑 `browser_check.py`、`training_flow_check.py`、`longitudinal_check.py`、`history_disclosure_check.py`、`body_map_check.py`、`rehab_check.py`、`onboarding_check.py`，均通過。包含實際 Chrome、IndexedDB、離線殼層、備份與受控 Google HTTP；不是實際 Google 帳號驗證。
- `atlas_ux_check.py` 與 `forest_ux_check.py` 通過。新增導覽入口曾導致 320px／200% 標題被遮住，已改用實際導覽高度設定捲動間距；保留原遮擋斷言並新增當日入口案例後通過。
- 已檢視合成資料及私人備份匯入後的手機尺寸畫面；私人截圖保留於 private/，未放入公開測試或文件。原生數字輸入模式已驗證，但 Android 實機鍵盤、切換 App 與長時間操作體感尚未驗證。本輪未部署，未新增照片、器材管理或跨重整草稿。動作圖是一般示意，並非特定機型照片。

## 2026-09-17：先前驗證紀錄

日期：2026-09-17。範圍：第一版程式、本機瀏覽器行為、受控的 Google 邊界與 GitHub Pages 部署。

## 已驗證

| 證據 | 結果 | 支持的結論 |
|---|---|---|
| `node --test tests/*.test.mjs` | 91 / 91 通過 | 資料驗證、版本圖、分析、備份、同步協調、肌群角色、持續記錄、InBody、動作計畫與 Sheet 擴充符合已列案例 |
| `node scripts/check-boundaries.mjs` | 42 個模組通過依賴檢查 | domain/storage/sync/UI 沒有越過設定的模組邊界 |
| `tests/browser_check.py` | 14 段流程通過 | 真正 Chromium、IndexedDB、service worker 和頁面互動可完成下列流程；Google HTTP 受控 |
| `tests/body_map_check.py` | 通過 | 三種圖層、單筆紀錄主協同上色、器材篩選、兩筆進步來源、空狀態、正背面、鍵盤、骨架、日期範圍、動作展開、320px／412px／200% 與離線切換 |
| `tests/atlas_ux_check.py` | 通過 | 焦點／展開保留、圖與進程一致、候選不受器材篩選影響、文字清單、明細跳轉、320/412px 於 200%、離線 |
| 私人起始資料流程 | 通過 | 正式備份合併、重複載入去重、待核對值隔離、來源保留、修改／匯出與離線讀回；私人測試資料不發布 |
| 最後程式審查 | 已修復發現的問題 | 父版本先同步、同日排序、建立後重複建立按鈕狀態已補回歸證據 |

Node 測試不是單纯檢查函式存在，涵蓋：

- 日期、未知欄位、各組重量、缺漏 InBody、疼痛／技術未知狀況。
- 同機台／同單位／同組數的保守比較；輔助量下降但次數下降不判進步；逐組總量手算驗證。
- 相同版本去重、同 ID 不同內容拒絕、缺父版本、循環、異類型、墓碑與並行版本解析。
- 修改舊紀錄不改首次建立順序；同日 InBody 與訓練依首次紀錄時間排序。
- 寫入成功但回應遺失的重試、同步途中新增、讀回失敗、雲端歷史被刪除、121 個版本的批次前綴完整性。
- RAW 寫入、Drive 清單分頁、損壞 metadata／表頭、401／429、GIS 取消、逾時與錯誤回應。

## 真正瀏覽器流程

使用 Google Chrome 的獨立設定檔，412 × 915 viewport、觸控／mobile 模式；本機 HTTP 以 `/rehab/` 子路徑測試 GitHub project Pages 路徑。

1. 新增訓練，重載後修改，確認真正 IndexedDB 保存。
2. 點器材卡會保留同器材設定、清空次數並產生新紀錄，不修改原始紀錄。
3. InBody 只填體重，其他測量仍為缺漏。
4. 透過頁面下載備份並再次匯入，版本不重複。
5. service worker 控制頁面後，斷網重載、新增、再重載仍可讀取。
6. 刪除後重載，墓碑生效且歷史保留。
7. 412px 頁面無水平溢出、沒有未處理的 JavaScript 錯誤；已檢視截圖。
8. 真實 UI → GIS adapter → sync engine → Sheets adapter → IndexedDB 串接；外部 GIS／HTTP 回應由測試控制。
9. 全新瀏覽器設定檔，從受控 Sheet 載入相同紀錄。
10. 兩個隔離設定檔離線修改同筆，恢復同步顯示衝突，選擇版本並讓另一設定檔讀回一致內容。
11. 兩個真正 IndexedDB 連線同時寫入，兩筆提交均保存。
12. 字體放大至 200%，頁面與訓練 dialog 在 412px 仍無水平溢出。

測試程式只替代 Google 外部回應，未替代 domain、儲存、同步或 UI 的 production code。這比 mock-only 單元測試多驗證模組間串接，但**仍不能證明真實 Google 服務、OAuth 設定或遠端持久化正常**。

截圖與測試紀錄在被 Git 忽略的 `artifacts/`。這些是虛構測試資料，不是使用者健身紀錄。

加入人體圖後重新執行上述兩支瀏覽器測試；已檢視正面／背面截圖，並重新檢查模組、Dashboard 串接及離線資產清單。人體圖只呈現動作關聯覆蓋，沒有證據可支持部位肌力或疼痛位置判讀。

## 尚未驗證／尚未部署

- GitHub repository、Actions 成功執行與正式 HTTPS 網址：尚未提供目標 repository。
- 真實 Google OAuth、建表、寫入後讀回、權杖失效及換機還原：尚未提供 Client ID／帳號環境。
- Android Chrome 真機的安裝、授權視窗、手機檔案備份与離線生命週期：目前只有 Chromium mobile viewport 證據。
- 瀏覽器／系統回收本機資料，以及 service worker 升級跨版本遷移，尚未做真機／長期測試。未同步資料不可視為已備份。
- 已整理使用者提供的文字紀錄，並驗證私人起始備份的完整匯入流程；原始 InBody 圖、機台與單位仍有待核對處，未假裝已經核實。瀏覽器連線工具沒有可用的使用者瀏覽器，因此沒有宣稱已將資料存入使用者日常瀏覽器／Android。

目前交付為**已實作並通過上述測試的第一版程式**，不是「已上線且可完全依賴的雲端系統」。外部設定請依 [setup.md](setup.md) 完成，再逐項做真實帳號與 Android 驗收。

## 審查修正與限制

- Google 批次附加可能中途失敗。同步引擎先依因果順序排列待送版本，使每個成功寫入的前綴都包含父版本。相同版本物理重複列仍可能存在，UI 依 ID＋內容去重，不宣稱 exactly-once。
- 同日優先採選填的台灣 time；缺少時間排在該日已填時間前，再依首次建立時間與 ID 排序。這是顯示排序，不代表未知紀錄的實際發生先後；日期、時間可補登，不自行推測。
- 手機資料綁定單一 Sheet，沒有加入跨資料集切換或自動合併不同帳號資料。
- GitHub 部署只使用允許清單的網站檔案；Google 權杖只留記憶體，測試備份與模擬權杖不包含於部署資產。


## 校準與來源資料擴充

先以測試重現「選單失去焦點、展開區收起、器材篩選改變整體候選」，三項皆由 false 修正為 true。兩處進步判讀共用同一比較模組，並補充可比組別數；只有一笔資料的疼痛／未知旗標不再被隱藏。小腿後側固定於背面。

新增資料品質案例驗證：逐欄待核對仍保留原始值、暖身不算工作訓練、來源與負荷語意經版本日誌和备份保留、未知欄位仍拒絕、舊 InBody 正規化內容不因新增可選欄位而改變、日期總覽按實際記錄統計。真實來源的長備註也揭露欄位標籤辨識問題，已用明確 aria-label 修正。

所有 Google 測試仍使用受控外部回應；沒有真實帳號／雲端寫入證據。真正 Android、教練動作校準、計時型復健紀錄、個人處方與圍度輸入仍未完成。一般截圖在 artifacts/，正式來源截圖與資料在 private/，兩者皆不發布。

## 持續記錄驗證

`tests/longitudinal_check.py` 使用虛構資料，實際操作訓練與 InBody 表單：沿用不同組重量並重設當次狀態、從歷史再練、三個不規則量測日期、56 天差值、30 天訓練篩選下仍保留 InBody 歷史、時間／條件／部位參考比的修改與保存、離線新增及重開、6 個版本還原成 5 筆紀錄、重複匯入去重、320/412px 表單無水平溢出。

另驗證 UTC 裝置仍採台灣日期、同日時間排序、待核對與排除值不計入差值、新欄位通過 Sheets adapter 的讀取與 RAW 寫出。私人來源重建器固定原匯入欄位集合，新增目錄欄位不改變 v1 事件內容。

目前手動填表；未儲存表單尚無跨重載草稿、報告圖片附件／OCR 與計時型動作尚未實作。不以這些自動化功能已存在作為驗收前提。

## 收合紀錄與精簡呈現

`tests/history_disclosure_check.py` 以 20 筆紀錄驗證紀錄庫預設關閉、原始數值／備註第二層展開、修改及重繪保留展開狀態、同步衝突在紀錄庫關閉時仍可處理、320/412px 在 200% 字體下不溢出。原有瀏覽器案例加上使用者展開紀錄庫的操作，保留原本的資料與同步斷言。

疼痛與下一步資料提醒預設收合；無紀錄部位僅提示先確認可訓練範圍。後續新增的動作計畫可將調整部位移出補基準候選，詳見動作計畫驗證。個人病史檔案依使用者要求不納入 app；不產生個人醫療處方或診斷。


## 動作計畫、狀況紀錄與精細人體圖

2026-09-16：89 項 Node 測試全數通過，39 個模組依賴檢查通過。重新執行 browser、rehab、body_map、atlas_ux、history_disclosure、longitudinal 及兩個私人來源驗證，全部通過。最後的人體圖連往動作計畫入口另由 rehab 實際瀏覽器案例確認。

- rehab 案例：嚴格欄位、不納入病史、unknown 不沿用舊陰性、空白追蹤不清除先前陽性、警訊日期與嚴重度一致、分數與位置圖、實際表單保存、離線重開、新瀏覽器備份還原。
- 私人新版入口：私人起始資料加一筆動作計畫，重複載入不增加紀錄。四張動作卡可編輯、直接開啟實際訓練紀錄；不預填完成次數。病史未出現在 DOM 或公開 src；私人資料不在 Git 與發布允許清單。
- 人體圖：增加肌群明暗、表面輪廓與關節線條；正背面調整虛線與填色互不覆寫。實際截圖確認正反面完整，320/412/1100px、100%/200% 字體無橫向溢出。小部位另有清單與鍵盤入口。
- Sheet：原子升級後才附加 rehab_log，舊表內容不動；受控 HTTP 的完整 UI／同步／新瀏覽器還原通過。升級失敗不附加、v2 缺表拒絕、授權錯誤維持原類別。原先「版本 2 不支援」的負向案例，依新增契約改為仍不支援的版本 3；未取消未知版本拒絕條件。

沒有真實 Google 帳號、GitHub Pages 部署或 Android 實機證據。運動卡是待個別確認的計畫選項，非遠端確診或安全核准；完整病史依使用者要求不納入 app。


## 舊快取入口更新修正

使用者回報私人新版入口只顯示「版本類型不正確」。已以真實 cache-first Service Worker 與舊版不接受 rehab 的驗證器重現：入口 HTML 從網路取得，但其靜態 import 仍載入舊快取。先前的全新瀏覽器案例不足以驗證此更新情境。

入口改為先等待 shell 更新與能力確認，再動態載入儲存／版本模組。v9 僅允許尚未載入 app 的私人入口，在同範圍沒有其他視窗時請求啟用新版；若仍有其他分頁，保留未儲存表單並提示儲存、關閉後重試。沒有 unregister、清除 IndexedDB 或移除紀錄；僅沿用原有殼層快取汰換。

`tests/shell_upgrade_check.py` 已由同訊息失敗轉為通過：舊快取更新、既有未同步紀錄與私人起始資料合併後仍待同步、其他分頁草稿保留、更新下載失敗不匯入且重試可恢復、更新後離線重開。另重跑私人新版來源驗證、完整 browser 流程、89 項 Node 測試與 39 模組邊界檢查通過。這是隔離的 Chromium 瀏覽器驗證，沒有直接檢視使用者瀏覽器的快取內容。


## 森林色系與日常閱讀優化

2026-09-16：呈現層配色集中於 src/ui/theme.css，頁面、SVG 圖表、人體圖使用語意變數；App 圖示由同一色票產生。v10 殼層納入新 CSS，已通過舊快取升級流程。資料驗證、計算和 Google 同步格式不因配色變動。

移除肌群明細及總覽的缺填數量催促、單筆來源中的未知疼痛／技術文字、尚未填寫今日狀況的提示。unknown 原始資料仍保留；已記錄的疼痛、代償、警訊與待核對量測值仍可見。新增四區就地導覽，章節標題及日期摘要精簡；手機 InBody 改單欄，人體標籤加大，比較規則展開查看。

89 項 Node 測試、39 個模組依賴檢查通過；forest_ux、browser、body_map、atlas_ux、rehab、history_disclosure、longitudinal、shell_upgrade 及兩個私人來源流程通過。森林 UX 案例驗證 5 筆 unknown 保留、不出現缺填催促、實際疼痛／代償可見、320/412/1100px 與 200% 字體不溢出、導覽跳轉不被頂部導覽遮住、記錄表單可操作。9 組主要文字與控制項的實際計算色彩對比均至少 5.57:1；不是完整 WCAG 符合性稽核。門檻參考 https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html 。

回歸時發現既有 history_disclosure 測試假設最後寫入必然排在最前，但第 18 與 19 筆 createdAt 同為 2026-09-16T05:00:09.560Z，既有穩定排序按 ID 決勝。已給虛構資料明確、遞增的觀察時間，保留全部展開／修改／衝突斷言；未改動產品排序或放寬驗證。

實際手機／桌面截圖保存在 private/，公開測試證據在 artifacts/forest-contrast.json。真實 Android 硬體及 Google 帳號仍未驗證。

## 訓練優先流程（v11）

2026-09-16：訓練感受收成選填、獨立不適追蹤移至結果後收合、器材結果可直接沿用，保存後結果維持展開。未修改 domain、storage 或 Google 同步格式。

新增 training_flow 真實 Chromium/IndexedDB 案例，先重現一般表單暴露感受欄位及存檔收起結果，再驗證：只填重量次數保存一筆 training、unknown 保留、A/kg 不誤沿用 B/lb、感受不另建 symptom、備註與疼痛出現在更新的結果及人體明細、圖上連結展開計畫、警訊在收合區域外可見、320/412/1100px 與 200% 字體不溢出。

本次 89 項 Node 與 39 模組邊界檢查通過；training_flow、rehab、browser（13 階段，Google 為受控 HTTP）、longitudinal、history_disclosure、forest_ux、shell_upgrade、body_map、atlas_ux、私人起始資料流程通過。實際手機視窗截圖：private/forest-home-phone.png、private/training-entry-phone.png。非 Android 實機或真實 Google 帳號驗證。

既有測試加入使用者展開選填區的操作，保留資料、離線、同步及衝突斷言。原計畫錨點測試要求距頂端小於 150px；移到頁底後實測 y=217.1px，整區仍在畫面內，瀏覽器已到最大捲動位置。改檢查目的摘要完整出現在可見範圍、未被導覽遮住且編輯入口可見，沒有取消可達性驗證。

## 手機上手與重新連線（v12）

2026-09-16：主畫面「同步」在尚未連線或未綁定時帶往既有設定流程，不主動彈出 Google 授權。新增 docs/start.html 手機指南並納入 Pages 白名單及離線快取；設定入口改連 HTML，技術 setup.md 保留。說明涵蓋 GitHub／Google 一次性設定、日常操作、舊預覽資料匯入正式網域、重新連線、換機及目前 Sheet 日誌不能任意手改的限制。

onboarding 實際瀏覽器測試先重現同步未帶入設定，再驗證專案子路徑、手機指南與返回入口、所有展開內容在 320/412/1100px 與 200% 字體可讀、離線開指南後回網頁保存。browser 現為 14 階段，新增重新載入丟失記憶體 token 後按同步→設定→Google 重新連線，保留 Client ID／原 Sheet，遠端筆數不增加。Google 回應仍為受控 HTTP，不能替代真實帳號授權。

89 項 Node 測試、39 模組依賴、onboarding、browser、training_flow、shell_upgrade 全數通過；實際 Pages 包裝白名單包含所有 Service Worker 快取資源且不含 private、artifacts 或 tests。工作區目前沒有 Git repository／正式部署，沒有 Android 真機或真實 Google 帳號驗收結果。

## 桌面與手機字級（v13）

2026-09-16：1440/1920px 實際量測的桌面正文由 16px 改為 18px；說明、導覽、圖例與狀態標籤統一約 16.875px（原約 12.8–14.4px）。手機正文保留 16px，次要標籤 15px；桌面 InBody 改兩欄，刻度及日期實際約 19.8px，320px 窄螢幕約 15.48px。人體圖文字實際桌面 19px、412px 手機 15.5px；320px 為 12.9px，另有 15px 的部位選單／數值清單可用。字級與導覽跳轉間距集中為 theme.css 變數，未改資料或分析。

一次性 typography_probe 使用私人起始資料量測、截圖並檢查 320/412/1440/1920px 的頁面與表單、100%/200% 文字；結果保留 artifacts/typography-before.json 與 typography-after.json，私人截圖 type-after-1440-home.png / type-after-412-home.png 等。forest_ux、atlas_ux、training_flow、rehab、shell_upgrade 回歸通過。驗證曾重現 320px/200% 導覽高度 135px、大於原跳轉間距 130.4px，已提高共用間距，原可見性斷言通過；未放寬測試。

已視覺檢查桌面總覽及 InBody 文字／日期排版。手機仍為瀏覽器尺寸模擬，非 Android 實機閱讀舒適度驗收。

## 新裝置 Google 預設設定（v14）

2026-09-17：正式部署在 HTML 公開設定中提供 OAuth Client ID。設定視窗以裝置既有值優先，沒有既有值時使用部署預設並自動準備授權；access token 仍只存在記憶體。新裝置只需按「連線 Google」，換機指南不再要求重貼 Client ID。Service Worker 新增設定模組並提升 shell 版本，避免離線更新混用舊模組。

先以新瀏覽器設定欄為空重現失敗，再驗證預設值存在、受控 Google 建表／同步／讀回成功，以及重新載入後保留裝置自行儲存的 Client ID。`tests/browser_check.py`、`tests/shell_upgrade_check.py`、`tests/onboarding_check.py`、89 項 Node 測試與 40 模組邊界檢查通過。GitHub Pages 已部署；真實 Google 帳號授權與 Android Chrome 操作仍需使用者端驗收。

## 依器材快速記錄（v15）

2026-09-18：首頁直接從既有訓練投影器材入口，不新增另一份需維護的常用清單。最近使用的 6 台先顯示，其餘收在「其他已記錄器材」；同日依動作目錄與機台名稱固定排序。點卡片會帶入動作、機台、單位、各組重量與組數，並清空本次次數、疼痛、動作狀況及備註，避免把上次結果當成這次紀錄。首次使用的器材從「其他器材」建立。

新增 2 項 domain 測試驗證同器材只取最新紀錄、排除待核對資料、最近優先與同日穩定排序。training_flow 另以 Chrome、IndexedDB 和 7 台器材驗證首層只顯示 6 台、其餘可展開、點卡後次數為空白，以及 320/412/1100px、200% 字體無橫向溢出。完整驗證結果以本節提交前最後一次執行為準；Android 實機操作仍需使用者端驗收。
